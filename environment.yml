name: rsan_env
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - setuptools
  - wheel
  - psutil
  - pip:
      # === Core ML/AI Libraries ===
      - torch==2.5.1
      - torchvision==0.20.1
      - torchaudio==2.5.1
      - fiftyone==1.4.2
      - opencv-python==4.10.0.84

      # === Dev Tools / Linting / Formatting ===
      - black==25.11.0
      - isort==6.1.0
      - flake8==7.3.0

      # === Scientific Stack ===
      - numpy==1.26.4
      - pandas==2.2.3
      - matplotlib==3.9.2
      - scikit-learn==1.5.2
      - scipy==1.14.1
      - tqdm==4.66.5
      - requests==2.32.3
      - PyYAML==6.0.2
      - pillow==10.4.0

      # === Optional: Utility / Stability Packages ===
      - python-dotenv==1.0.1
      - rich==13.9.2
      - typing-extensions==4.12.2
      - packaging==25.0
      - pathspec==0.12.1